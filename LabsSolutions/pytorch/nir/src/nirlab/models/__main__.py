# coding: utf-8

# Standard import
import logging
import sys

# External imports
import torch

if __name__ == "__main__":
    logging.basicConfig(stream=sys.stdout, level=logging.INFO, format="%(message)s")
